/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['media.istockphoto.com', 'i.imgur.com', 'cdn.pixabay.com', 'ssl.pstatic.net']
  }
};

module.exports = nextConfig;
